
{# app/Resources/views/Meetup/create.html.twig #}
{{ form_start(form) }}
    {{ form_row(form.district) }}    {# <select id="meetup_sport" ... #}
    {{ form_row(form.county) }} {# <select id="meetup_position" ... #}
    {# ... #}
{{ form_end(form) }}
{#
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js">
var $district = $('#student_district');
// When sport gets selected ...
$district.change(function() {
  // ... retrieve the corresponding form.
  var $form = $(this).closest('form');
  // Simulate form data, but only include the selected sport value.
  var data = {};
  data[$district.attr('name')] = $district.val();
  // Submit data via AJAX to the form's action path.
  $.ajax({
    url : $form.attr('#'),
    type: $form.attr('POST'),
    data : data,
    success: function(html) {
      // Replace current position field ...
      $('#county_name').replaceWith(
        // ... with the returned one from the AJAX response.
        $(html).find('#student_county')
      );
      // Position field now displays the appropriate positions.
    }
  });
});
</script> #}